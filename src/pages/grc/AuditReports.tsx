import PageShell from "@/components/layout/PageShell";

const AuditReports = () => {
  return (
    <PageShell
      title="Audit Reports"
      description="Audit engagement summaries, control observations, evidence mapping, and remediation tracking outputs will be presented here."
    />
  );
};

export default AuditReports;
